# Vercel server-side values. Never expose RESULTS_WRITE_TOKEN with a VITE_ prefix.
APPS_SCRIPT_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
RESULTS_WRITE_TOKEN=replace-with-a-long-random-secret
